import { Callout } from "vocs/components";

<Callout type="warning">
  **Deprecated pattern.** This page documents the legacy `gpt-4o-audio-preview`
  single-call approach, which is superseded by the
  [VoiceAgentAdapter](/voice/getting-started) system shipped in #350 / #355.
  The page still works and existing code remains valid, but
  [Voice Agents → Getting Started](/voice/getting-started) is now the
  recommended entry point.
</Callout>
